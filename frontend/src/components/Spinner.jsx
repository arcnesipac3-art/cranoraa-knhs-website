import React from 'react';

/**
 * Legacy Spinner component — redirects to LoadingSpinner for consistency.
 * New code should import from '../components/ui' instead.
 */
export { LoadingSpinner as Spinner, LoadingSpinner as default } from './ui/LoadingSpinner';
